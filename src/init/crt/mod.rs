// Init: C runtime stub — re-exported from the canonical location.
// Source file lives in `src/crt/crt0.c` (compiled separately by the build system).
// This module exists so `crate::init::crt` resolves for future Rust wrappers.
